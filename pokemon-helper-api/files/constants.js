import path from "path";

export const FIRST_FILE_PATH = path.join(
  process.cwd(),
  "..",
  "storage",
  "first.csv"
);
export const SECOND_FILE_PATH = path.join(
  process.cwd(),
  "..",
  "storage",
  "second.csv"
);
export const NEED_CARDS_PATH = path.join(
  process.cwd(),
  "..",
  "storage",
  "need.csv"
);
export const PRICES_CARDS_PATH = path.join(
  process.cwd(),
  "..",
  "storage",
  "prices.csv"
);

export const SALE_CARDS_PATH = path.join(
  process.cwd(),
  "..",
  "storage",
  "sale.json"
);
